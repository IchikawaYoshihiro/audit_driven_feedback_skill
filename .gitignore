# skill-creator eval run artifacts (machine-specific scratch, not part of the skill)
*-workspace/
**/iteration-*/
feedback.json

# install bundles are built by CI and attached to Releases, not committed
*.zip

# OS / editor
.DS_Store
Thumbs.db
